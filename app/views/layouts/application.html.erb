<!DOCTYPE html>
<html>
  <head>
    <title>RabbitLovers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <script src="https://kit.fontawesome.com/a6ef0cdc17.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    <link href="https://fonts.googleapis.com/css?family=M+PLUS+1p" rel="stylesheet">
  </head>

  <body class="d-flex flex-column vh-100">
    <header>
      <div class="container" style="background-color: #d2d8d8;">
        <nav class="navbar navbar-expand-lg navbar-light pr-3 pb-3">
          <%= link_to image_tag("logo.jpg", width: '100%', height: '100%', class: "navbar-brand"), root_path %>
          <button class="navbar-toggler mb-2" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
              <% if user_signed_in? %>
                <li class="nav_item"><%= render 'users/shared/after_sign_in_users' %></li>
              <% elsif admin_signed_in? %>
                <li class="nav-item"><%= render 'admins/shared/after_sign_in_admins' %></li>
              <% else %>
                <li class="nav_item"><%= render 'users/shared/before_sign_in' %></li>
              <% end %>
            </ul>
          </div>
        </nav>
      <% if user_signed_in? %>
        <div class="row">
          <div class="col-lg-12">
            <div class="text-right mr-3 mb-3"><span class="marker">welcome, <%= current_user.name %>さん！</span></div>
          </div>
        </div>
      <% end %>
    </header>
    <main>
      <div class="container mb-3 mt-2 ">
        <div class="row">
          <div class="pl-3">
            <% flash.each do |message_type, message| %>
              <div class="flash-message <%= message_type %>"><%= message %></div>
            <% end %>
          </div>
        </div>
      </div>
      <%= yield %>
    </main>
    <footer>
      <div class="container mb-3 mt-2 ">
        <div class="row">
          <div class="mx-auto">
            <p>CopyRight © 2023 RabbitLovers Inc.</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
